<%= form_for(@artist) do |f| %>
  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% @artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="well">
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
</div>
<div class="well">
  <div class="field">
    <strong>Upload Image: </strong>
    <%=f.file_field :avatar %><br>
  </div>
</div>

<div class="field">
  <%= f.select(:rating, @ratings.collect { |r| [ r ] }, {include_blank: "Rate It!"} ) %><br>
</div>
 

  <div class="actions">
    <%= f.submit %><br>
  </div>
<% end %>

